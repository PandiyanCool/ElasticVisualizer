Works planned:

Primary:
1.	monitor snapshot functionality
2.	shard relocating status
3.	optimization - number of segments (current index size & expected size after optimization)
4.	handling alias across indexes
5.	CPU usage status
6.	indices status (green, yellow, red)

Secondary:
1.	document count (index, shard, node wise)
2.	space availability in cluster

Optional:
1.	warmer
2.	split brain warning
3.	configuration checklist
	